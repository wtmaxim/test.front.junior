import { IDestination } from './destination.model';

export const paris = {
	id: 'paris',
	name: 'paris',
} as IDestination;
export const marseille = {
	id: 'marseille',
	name: 'marseille',
} as IDestination;
